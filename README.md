# Harding-Film-Manager
A script that is input into a Google Sheet that works in line with Google Calendar and Google Forms to automatically confirm equipment checkout requests and adjust the inventory accordingly.
